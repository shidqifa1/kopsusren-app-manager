DATABASE_URL=postgresql://user:password@host:5432/dbname
AUTH_SECRET=your-random-secret-key-change-this
VALID_USERNAME=shidqifa
VALID_PASSWORD_HASH=salt:scrypt_hash_64_bytes_hex
# Untuk generate VALID_PASSWORD_HASH, jalankan:
# node -e "const{crypto}=require('crypto');const s=crypto.randomBytes(16).toString('hex');const h=crypto.scryptSync('passwordkamu'+'kopsusren-app-2024',s,64).toString('hex');console.log(s+':'+h)"
