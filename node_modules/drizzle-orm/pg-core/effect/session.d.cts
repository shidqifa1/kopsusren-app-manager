import { SelectedFieldsOrdered } from "../query-builders/select.types.cjs";
import { PgDialect } from "../dialect.cjs";
import { PgBasePreparedQuery, PgQueryResultHKT, PgSession, PgTransactionConfig, PreparedQueryConfig } from "../session.cjs";
import { PgEffectDatabase } from "./db.cjs";
import { entityKind } from "../../entity.cjs";
import { Query, SQL } from "../../sql/sql.cjs";
import * as V1 from "../../_relations.cjs";
import { MigrationConfig, MigrationMeta } from "../../migrator.cjs";
import { WithCacheConfig } from "../../cache/core/types.cjs";
import { AnyRelations, EmptyRelations } from "../../relations.cjs";
import * as Effect from "effect/Effect";
import { EffectCache } from "../../cache/core/cache-effect.cjs";
import { SqlError } from "@effect/sql/SqlError";
import { EffectDrizzleQueryError, EffectTransactionRollbackError, MigratorInitError } from "../../effect-core/errors.cjs";
import { QueryEffectHKTBase, QueryEffectKind } from "../../effect-core/query-effect.cjs";

//#region src/pg-core/effect/session.d.ts
declare abstract class PgEffectPreparedQuery<T extends PreparedQueryConfig, TEffectHKT extends QueryEffectHKTBase = QueryEffectHKTBase> extends PgBasePreparedQuery {
  private cache;
  private queryMetadata;
  private cacheConfig?;
  static readonly [entityKind]: string;
  constructor(query: Query, cache: EffectCache, queryMetadata: {
    type: 'select' | 'update' | 'delete' | 'insert';
    tables: string[];
  } | undefined, cacheConfig?: WithCacheConfig | undefined);
  protected queryWithCache<A, E, R>(queryString: string, params: any[], query: Effect.Effect<A, E, R>): Effect.Effect<A, EffectDrizzleQueryError, Exclude<R, EffectCache>>;
  abstract execute(placeholderValues?: Record<string, unknown>): QueryEffectKind<TEffectHKT, T['execute']>;
}
declare abstract class PgEffectSession<TEffectHKT extends QueryEffectHKTBase = QueryEffectHKTBase, TQueryResult extends PgQueryResultHKT = PgQueryResultHKT, TFullSchema extends Record<string, unknown> = Record<string, never>, TRelations extends AnyRelations = EmptyRelations, TSchema extends V1.TablesRelationalConfig = V1.ExtractTablesWithRelations<TFullSchema>> extends PgSession {
  static readonly [entityKind]: string;
  constructor(dialect: PgDialect);
  abstract prepareQuery<T extends PreparedQueryConfig = PreparedQueryConfig>(query: Query, fields: SelectedFieldsOrdered | undefined, name: string | undefined, isResponseInArrayMode: boolean, customResultMapper?: (rows: unknown[][], mapColumnValue?: (value: unknown) => unknown) => T['execute'], queryMetadata?: {
    type: 'select' | 'update' | 'delete' | 'insert';
    tables: string[];
  }, cacheConfig?: WithCacheConfig): PgEffectPreparedQuery<T, TEffectHKT>;
  abstract prepareRelationalQuery<T extends PreparedQueryConfig = PreparedQueryConfig>(query: Query, fields: SelectedFieldsOrdered | undefined, name: string | undefined, customResultMapper: (rows: Record<string, unknown>[], mapColumnValue?: (value: unknown) => unknown) => T['execute']): PgEffectPreparedQuery<T, TEffectHKT>;
  execute<T>(query: SQL): QueryEffectKind<TEffectHKT, T>;
  all<T>(query: SQL): QueryEffectKind<TEffectHKT, T[]>;
  abstract transaction<A, E, R>(transaction: (tx: PgEffectTransaction<TEffectHKT, TQueryResult, TFullSchema, TRelations, TSchema>) => Effect.Effect<A, E, R>): Effect.Effect<A, E | SqlError, R>;
}
declare abstract class PgEffectTransaction<TEffectHKT extends QueryEffectHKTBase, TQueryResult extends PgQueryResultHKT, TFullSchema extends Record<string, unknown> = Record<string, never>, TRelations extends AnyRelations = EmptyRelations, TSchema extends V1.TablesRelationalConfig = V1.ExtractTablesWithRelations<TFullSchema>> extends PgEffectDatabase<TEffectHKT, TQueryResult, TFullSchema, TRelations, TSchema> {
  protected relations: TRelations;
  protected schema: {
    fullSchema: Record<string, unknown>;
    schema: TSchema;
    tableNamesMap: Record<string, string>;
  } | undefined;
  protected readonly nestedIndex: number;
  static readonly [entityKind]: string;
  constructor(dialect: PgDialect, session: PgEffectSession<TEffectHKT, any, any, any, any>, relations: TRelations, schema: {
    fullSchema: Record<string, unknown>;
    schema: TSchema;
    tableNamesMap: Record<string, string>;
  } | undefined, nestedIndex?: number, parseRqbJson?: boolean);
  rollback(): EffectTransactionRollbackError;
  setTransaction(config: PgTransactionConfig): QueryEffectKind<TEffectHKT, void>;
}
declare const migrate: <TEffectHKT extends QueryEffectHKTBase>(migrations: MigrationMeta[], session: PgEffectSession<TEffectHKT, PgQueryResultHKT, Record<string, never>, EmptyRelations, V1.ExtractTablesWithRelations<Record<string, never>>>, config: string | MigrationConfig) => Effect.Effect<undefined, SqlError | MigratorInitError | TEffectHKT["error"], TEffectHKT["context"]>;
//#endregion
export { PgEffectPreparedQuery, PgEffectSession, PgEffectTransaction, migrate };
//# sourceMappingURL=session.d.cts.map